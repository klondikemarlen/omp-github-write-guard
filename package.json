{
  "name": "omp-github-write-guard",
  "version": "0.1.1",
  "description": "Opt-in OMP guard for GitHub writes outside the current checkout.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "release:check": "bun test",
    "reinstall": "omp plugin install github:klondikemarlen/omp-github-write-guard"
  },
  "keywords": ["omp-plugin", "github", "safety"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klondikemarlen/omp-github-write-guard.git"
  },
  "omp": {
    "extensions": ["./index.ts"],
    "settings": {
      "issueCreationPolicies": {
        "type": "string",
        "default": "{}",
        "description": "JSON map of exact owner/repository targets to allow or confirm for issue creation."
      },
      "pullRequestCreationPolicies": {
        "type": "string",
        "default": "{}",
        "description": "JSON map of exact owner/repository targets to allow or confirm for pull-request creation."
      }
    }
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
